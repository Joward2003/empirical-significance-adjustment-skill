# Skill设计报告

## 1. 采用的GitHub结构

本Skill采用开放Agent Skills常见结构：根目录`SKILL.md`负责触发和主工作流，较长的方法知识放入`references/`，可重复与确定性的任务放入`scripts/`，输入输出模板放入`assets/`，测试提示放入`evals/`。

同时参考实证研究Skill将方法选择、执行、稳健性和报告分离的做法，但本项目保持为单一Skill：主文件负责路由，方法注册表负责全部方法，脚本负责审计与报告。

## 2. 常用操作维度划分

| 维度 | 主要内容 |
|---|---|
| D00 | 企业/省份/城市/国家基准模板 |
| D01 | 聚类、双向聚类、robust、wild bootstrap |
| D02 | 控制变量组与逐步报告 |
| D03 | 标准化与标准化beta |
| D04 | 企业、行业×年、省份×年、区域×年固定效应 |
| D05 | 起止年份、特殊年份、滚动窗口 |
| D06 | 滞后一期、两期、全部滞后、前置结果 |
| D07 | 对数、二次项、IHS、缩尾 |
| D08 | 1-4位行业代码和行业固定效应 |
| D09 | 分位数、DID、RDD、门槛、PSM、Heckman、IV、SCM |
| D10 | 阈值、中位数、区间、三分位赋值 |
| D11 | 五类核心概念替代代理 |
| D12 | 七类综合指数/降维方法 |
| D13 | ST企业两种处理和配对方案 |
| D14 | 倒数、上界减原值、归一化补数等正向化 |
| D15 | 企业、行业、地区和特殊样本策略 |

## 3. 运行产物

- `adjustment_plan.md`：执行前的候选方法计划
- `baseline_snapshot.json`：不可覆盖的基准快照
- `lock_baseline_result.py`：将完整基准估计一次性封存，并拒绝后续覆盖
- `adjustment_log.jsonl`：每次尝试的追加式日志
- `report.md`：完整结果与相对基准变化
- Stata `.do`、回归表和图形：由用户端Stata生成

## 4. 当前版本边界

- 已完整结构化PDF中的方法、选项和示例代码。
- Python脚本负责配置、日志和报告，不直接运行Stata。
- 方法注册表中的Stata代码含占位符，Agent需要根据项目配置替换。
- 部分命令如`reghdfe`、`winsor2`、`boottest`、`xthreg`、`rdrobust`、`synth`等需用户Stata环境另行安装。
